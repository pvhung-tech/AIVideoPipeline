{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop permissions for the main application window.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
